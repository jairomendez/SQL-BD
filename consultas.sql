SELECT * FROM `instituciones`
	-muestra la información de todas las instituciones registradas en la tabla.

SELECT * FROM `instituciones` WHERE municipio="tierralta"
	-muestra las instituciones registradas en un 	municipio especifico.

SELECT * FROM `instituciones` WHERE nombre="institucion educativa las delicias" 
	-arroja la información de una intitucion 	especifica.

SELECT * FROM `personas`
	-muestra todos los usuarios registrados